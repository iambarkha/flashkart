spring:
  application:
    name: user-service
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

server:
  port: 8081

logging:
  level:
    root: INFO
    com.flashkart: DEBUG

jwt:
  secret: test-secret-key-for-jwt-token-testing-that-is-very-long-and-secure-for-hs256-algorithm
  expiration: 3600000

